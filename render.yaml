services:
  # Web Service
  - type: web
    name: okcrisis
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn pythonanywhere_app:app
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: NEWSDATA_API_KEY
        sync: false
